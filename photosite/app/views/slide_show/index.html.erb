<% image = @user.uploads %>
<div class="bxslider">
  	<% image.all.each do |image| %>
		<%=cl_image_tag(image["public_id"],
		:height => 450,
		:width => 750,
		:crop => :fill)
	%>
	<% end %>
</div>

